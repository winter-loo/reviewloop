{
	"name": "ltsql-review-platform",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "vite dev",
		"build": "vite build",
		"preview": "vite preview",
		"prepare": "svelte-kit sync || echo ''",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
		"test": "vitest run",
		"build:cli": "esbuild src/cli/main.ts --bundle --platform=node --format=esm --target=node24 --outfile=dist-cli/reviewctl.js --banner:js='#!/usr/bin/env node' && chmod +x dist-cli/reviewctl.js && cp dist-cli/reviewctl.js dist-cli/ltsql-review.js",
		"package:review-platform": "npm run check && npm test && npm run build && npm run build:cli && bash scripts/package-ltsql.sh",
		"package:ltsql": "npm run package:review-platform"
	},
	"bin": {
		"review": "./bin/review.js",
		"reviewctl": "./bin/reviewctl.js",
		"ltsql-review": "./bin/ltsql-review.js"
	},
	"devDependencies": {
		"@sveltejs/adapter-node": "^5.5.4",
		"@sveltejs/kit": "^2.57.0",
		"@sveltejs/vite-plugin-svelte": "^7.0.0",
		"@types/markdown-it": "^14.1.2",
		"esbuild": "^0.28.0",
		"svelte": "^5.55.2",
		"svelte-check": "^4.4.6",
		"tsx": "^4.22.3",
		"typescript": "^6.0.2",
		"vite": "^8.0.7",
		"vitest": "^4.1.7"
	},
	"dependencies": {
		"docx-preview": "^0.4.0",
		"markdown-it": "^14.2.0",
		"pdfjs-dist": "^6.3.289"
	}
}
