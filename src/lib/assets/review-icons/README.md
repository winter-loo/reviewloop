# ReviewLoop review UI icons

SVG assets exported from the official Figma remote MCP design context for
[ReviewLoop · Toolbar & Mobile Review UI](https://www.figma.com/design/LDI4UokYh2sWhVG4P0dFQf).

Import with `?url` so Vite embeds or fingerprints the asset through the existing
application asset path. The production proxy does not serve arbitrary static
root directories.
